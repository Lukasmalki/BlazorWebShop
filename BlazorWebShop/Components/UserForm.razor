@* @using ClassLibrary
@inject NavigationManager NavigationManager

@* <form class="form" OnValidSubmit="HandleSubmit">
	<div>
		<label for="fullName">Fullständigt namn</label>
		<input @bind="userInfo.Fullname" type="text" id="fullName" name="fullName">

		<label for="streetAdress">Address</label>
		<input type="text" id="streetAdress" name="streetAdress">
	</div>

	<div>
		<label for="postalCode">Postkod</label>
		<input type="number" id="postalCode" name="postalCode">

		<label for="city">Stad</label>
		<input type="text" id="city" name="city">
	</div>
	<button type="submit">Submit</button>

</form> *@

@* <EditForm Model="@userInfo" OnValidSubmit="HandleSubmit">
	<DataAnnotationsValidator />
	<ValidationSummary />

	<div>
		<label for="fullName">Full Name</label>
		<InputText @bind-Value="userInfo.Fullname" id="fullName" />
	</div>

	<div>
		<label for="streetAdress">Address</label>
		<InputText @bind-Value="userInfo.StreetAdress" id="streetAdress" />
	</div>

	<div>
		<label for="postalCode">Postal Code</label>
		<InputNumber @bind-Value="userInfo.PostalCode" id="postalCode" />
	</div>

	<div>
		<label for="city">City</label>
		<InputText @bind-Value="userInfo.City" id="city" />
	</div>

	<button type="submit">Submit</button>
</EditForm> *@


@* @code {
	[Parameter] public EventCallback OnSubmit { get; set; }

	private UserInfo userInfo = new UserInfo();
	private void HandleSubmit()
	{
		NavigationManager.NavigateTo("/confirmorder");
	} *@

@* 	// private UserInfo userInfo = new UserInfo();

	// private void HandleSubmit()
	// {
		// Navigate to the confirmation page and pass the user input as query string parameters
		// NavigationManager.NavigateTo($"/confirmorder?fullname={userInfo.Fullname}&address={userInfo.StreetAdress}&postalcode={userInfo.PostalCode}&city={userInfo.City}");
			// NavigationManager.NavigateTo("confirmorder");

	// }
}
 *@